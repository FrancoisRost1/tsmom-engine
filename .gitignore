__pycache__/
*.pyc
.pytest_cache/
data/cache/
data/raw/
# data/processed/ is intentionally tracked — the Streamlit dashboard loads
# pre-computed backtest CSVs from there, so they must ship with the repo.
outputs/
.DS_Store
*.egg-info/
